# Changelog

## 0.4.0

- 新增由权威任务状态驱动的分阶段 `NEXT` 引导，明确当前目标、输入示例、授权循环，并在中央交汇舱解释完整修复与代价旁路两条路线。
- 输入区新增可点击关键词名片，按当前阶段与可执行动作动态更新；点击只填入光标位置，不会自动发送或越过授权。
- 本地与在线 NPC 同步加入虚假时间跳跃防护；玩家输入“过了一年”等叙述不会改写世界状态，NPC 会依据设施计时与当前位置纠正。
- 右栏内容改为内部滚动，确保 720p 下候选授权卡、关键词和输入框同时可见。
- 修正 1280/1366 宽窗口仍使用宽屏三栏尺寸而裁切右侧调度栏的问题；响应式判断改用真实可见视口，并保留 350 像素设施栏。
- 新增独立 `NpcContextCompiler`：把角色核心、场景模式、局部事实、主观信念、记忆、关系、导演意图和最近对话分区编译。
- 调度员读数只以带来源与置信度的 `unverified_claim` 进入林岚信念，不再与亲眼确认事实混用。
- 新增上下文分区预算、硬过滤记录、稳定事实/记忆 ID、模型引用 ID、Prompt hash 与最近 64 轮 trace。
- Prompt trace 保留在调试平面，不进入角色认知；客户端不再上传重复的完整原始 context。
- Responses API 严格输出协议加入 `referenced_ids`，服务器继续对动作与目标做白名单校验。
- 新增生成后质量门：剥离虚假通信故障话术，拦截高置信事实遗漏和引用事实矛盾，并把触发原因写入 trace。
- Headless 视觉测试改为 UI 状态冒烟模式并自动退出；有显示环境仍保存真实截图。
- 左侧设施栏扩宽，任务/系统事件区接管纵向剩余空间；中央通讯记录不再混入 SYSTEM 事件。
- 设施地图改为中央交汇舱向四条走廊放射，逃生舱联锁线路不再被主电网节点遮挡。
- AI/上下文回归覆盖扩展至 Python 23 项、Godot 核心 217 项。
- 主界面集成回归扩展至 89 项。
- 新增 Windows 一键发行包：入口 EXE 自动启动/回收内置 Python AI 代理，玩家无需安装 Godot 或 Python。
- 新增隔离 ZIP 冒烟测试，覆盖文件哈希、内置配置、真实在线 NPC 回复、游戏启动和代理进程树清理。

## 0.3.0

- 将电网与冷却规则拆分为独立谜题模块；冷却改为可逆的目标压力组合推理。
- 新增应急旁路电芯和便携氧气罐，形成完整修复、代价旁路与补给取舍路线。
- 信任与恐惧开始影响危险操作意愿；安抚可准备聚焦细扫，连续对话会推进通讯耗氧周期。
- 新增可点击的远端/现场双轨线索工作台，以及安全候选 Enter 快速授权设置。
- 新增房间前景层、现场特写框、关系化姿态与更明确的操作演出。
- 客户端拒绝多目标动作的空目标候选，服务端不再向模型转发被关系状态禁用的动作。
- 核心回归扩展至 198 项，主流程集成扩展至 74 项，Python 服务测试扩展至 16 项。

## 0.2.0

- 每次重开随机生成电缆读数、正确闭环与 I/B/P 管路映射。
- 新增本地多轮对话记忆、玩家姓名/承诺、NPC 信任、恐惧与信念状态。
- 修复否定、条件和疑问句被误判为动作的问题。
- 修复核心事件在通讯日志中重复出现的问题。
- 危险候选改为一次明确确认；保留核心直接调用的二次确认保护。
- 新增在线取消、失败熔断、固定离线模式、Origin 限制、限流与健康指标。
- 新增程序化无线电环境音和事件音效。
- 新增字号、音量、静音、减少动态和在线模式设置，以及窄屏布局适配。
- 新增关系化任务简报、事故签名、情境提示、Windows 导出预设与发布文档。
